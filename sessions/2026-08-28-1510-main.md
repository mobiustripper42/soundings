---
session: 13
slug: main
branch: main
started: 2026-08-28T15:10:08Z
ended:
points:
pr_numbers: [90]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/bd572455-450a-5ffb-a233-73c9ebc4740a.jsonl
---

# Session 13 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: One piece gains "One claim per sentence"

**Completed:**
- `.claude/output-styles/one-piece.md` — new section between "Fewer claims, not fewer
  words" and "Lead with the answer". Names the mechanism the density section describes
  but never gave an instruction for: split a sentence carrying two facts into two
  sentences. The argument is that "make it shorter" is satisfiable by deleting sentences
  while keeping every fact, which raises density; "more sentences per fact" is not.
- Authored by the operator, added verbatim. Destined for jig and the other repos, same
  path as PR #88.

**Code review:** 2 findings, both consistency, neither blocking — **and worth a decision
before merge.** The new section breaks the file's own "One name per thing" rule: `claim`
is already defined at line 14 at the *turn* level ("one thing the user could disagree
with"), and the new title plus its closing line use it at the *sentence* level, while the
section's own body says "fact" in between. Second finding follows from the first — "No
fact is dropped" reads as fighting the neighbouring section's "carry less" remedy, and the
two only reconcile if the two units have different names. Reviewer's fix is to say "fact"
throughout and retitle to **One fact per sentence**. Left as authored; the wording is the
operator's call. `/security-review` not run — prompt document, no blast-radius trigger.

**Proof:** `npm run verify` green — decisions, dictionary (9 terms, 16 files gated),
denied (63 prefixes), context, docs (15). The 6 dictionary warnings are pre-existing
grandfathered alternates. No gate covers `.claude/output-styles/`, so the green is about
the rest of the repo staying intact rather than about this file.

**PR:** [PR #90](https://github.com/mobiustripper42/soundings/pull/90)
**Points:** 1 — one section in one file, but the review finding is the real content.
**Branch:** task/one-piece-one-claim-per-sentence
**Opened at:** 2026-08-28T16:42:00Z

**Next Steps:**

**Context:**
