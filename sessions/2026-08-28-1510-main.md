---
session: 13
slug: main
branch: main
started: 2026-08-28T15:10:08Z
ended:
points:
pr_numbers: [90, 92]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/bd572455-450a-5ffb-a233-73c9ebc4740a.jsonl
---

# Session 13 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: One piece gains "One fact per sentence"

**Completed:**
- `.claude/output-styles/one-piece.md` — new section between "Fewer claims, not fewer
  words" and "Lead with the answer". Names the mechanism the density section describes
  but never gave an instruction for: split a sentence carrying two facts into two
  sentences. The argument is that "make it shorter" is satisfiable by deleting sentences
  while keeping every fact, which raises density; "more sentences per fact" is not.
- Authored by the operator. Destined for jig and the other repos, same path as PR #88.

**Code review:** two rounds, three findings, all fixed.
- Round 1 — the section broke the file's own "One name per thing" rule. `claim` was
  already defined at line 14 at the *turn* level ("one thing the user could disagree
  with"), and the new title plus its closing line used it at the *sentence* level while
  the body said "fact" in between. Second finding followed: "No fact is dropped" read as
  fighting the neighbouring section's "carry less" remedy, since the two remedies only
  reconcile when the two units have different names. Fixed in `3983a99` — retitled to
  **One fact per sentence**, "fact" throughout, plus one closing line making the boundary
  explicit.
- Round 2 — re-review confirmed both resolved, then found the same collision recurring on
  the other word. Line 22's pre-existing "four load-bearing **facts**" counts what a turn
  carries. Fixed in `90fd717`.
- Final split, no overlap: `claim` turn-level at 15/18/22/23/43, `fact` sentence-level at
  35/36/39/40.
- `/security-review` not run — prompt document, no blast-radius trigger.

**Proof:** `npm run verify` green after each of the three commits — decisions, dictionary
(9 terms, 16 files gated), denied (63 prefixes), context, docs (15). The 6 dictionary
warnings are pre-existing grandfathered alternates. No gate covers
`.claude/output-styles/`, so the green is about the rest of the repo staying intact rather
than about this file; the term split was checked by grep instead.

⚠ `gh pr edit` fails in this repo with a Projects-classic GraphQL deprecation error, the
same one that breaks `gh issue view`. `gh api -X PATCH repos/{owner}/{repo}/pulls/{n}`
works and is the way to retitle or re-body a PR here.

**PR:** [PR #90](https://github.com/mobiustripper42/soundings/pull/90)
**Points:** 1 — one section in one file, but the review finding is the real content.
**Branch:** task/one-piece-one-claim-per-sentence
**Opened at:** 2026-08-28T16:42:00Z

## Task 2: Phase 2.2 — Watermark tension math, gateway-side (issue #20)

**Completed:**
- `gateway/soundings_gateway/tension.py` — the SPEC §5.1 curve, pure, no I/O. Coefficients
  are a `WatermarkCoefficients` parameter defaulting to Irrometer, because §5.1 puts
  commercial and homemade sensors as matched pairs on ONE node and a homemade block does
  not follow Irrometer's curve. No config plumbing (DEC-001); the seam is the signature.
- `contracts/tools/gen_tension_vectors.py` + `contracts/vectors/tension-v1.json` — 18
  vectors, generator transcribes the equation independently of the implementation. Cases
  cluster in the operator's own 25-30 cb trigger / 50-60 cb stress bands rather than
  spreading evenly to 239 cb.
- `derive_bed` + a `derive_reading` role dispatcher in `derive.py`; `publish.py` and
  `spine.py` now call the dispatcher. Two synthetic bed nodes in
  `gateway/config/soundings.toml`, marked as sim-only.
- `docs/SPEC.md` §5.1 — removed the stale "[deferred]" on where the math runs (D1 resolved
  it gateway-side 2026-07-10) and documented the pole. `contracts/publish-v1.md` — derived
  keys and refusals per role.
- **Scope: the issue splits and half is blocked.** AC-excitation sampling needs the
  ADC-counts→kΩ constant, set by a divider ratio on a circuit that does not exist (D11,
  open). Estimate 3, down from the issue's 5.

**Code review:** 1 finding, contract-level, fixed in `5b036a4`. `TENSION_TEMP_PAIR` is
load-bearing for correctness and the binding under it was unpinned — `packet-v1.md` said
bit 1 was the "second Watermark (**e.g.** 12\")" and bits 2/3 had no depth at all, so a
future bed manifest could wire the deep homemade block onto bit 2 without violating
anything written down. That does not fault and does not drop: it compensates from the
wrong depth and publishes a confident wrong number with every wire field individually
valid. Pinned in the channel registry as shallow/deep co-location rather than inches.
Reviewer confirmed the rest clean — guards, generator agreement, positive controls, no
other dependents on the emitter values. `/security-review` not run: no money, no auth, no
migration, and this repo has no database.

**Proof:** 295 gateway tests (was 265), `npm run verify` 5 gates green.
Hand-computed anchors written before the implementation; the independent generator agrees
to 3 decimals. **Mutation-checked both modules** — `tension_kpa` → `None` fails 13/18,
`_derive_bed` → `[]` fails 12/12. No firmware touched, so no `pio test -e native`.

**Surface check:** drove a synthetic bed node through the real config file. It caught the
finding of the session: `emitter.py`'s drydown ran 12 kΩ → 200 kΩ, chosen against the
`0xFFFE` **encoding** ceiling rather than the sensor's range. 200 kΩ is far past the pole,
so every reading after day zero derived nothing; 12 kΩ is 65 cb, so the "freshly watered"
node opened its drydown already past the stress threshold. **The fake was never wrong
about anything until something consumed it.** Both ends now physical; the sim spans 9.5 cb
to 202 cb, crossing both decision bands.

**PR:** [PR #92](https://github.com/mobiustripper42/soundings/pull/92)
**Points:** 3
**Branch:** task/2.2-watermark-tension-math
**Opened at:** 2026-08-28T18:05:00Z

**Next Steps:**

**Context:**
