---
session: 12
slug: main
branch: main
started: 2026-08-28T03:19:29Z
ended: 2026-08-28T14:58:10Z
points: 8
pr_numbers: [87]
status: closed
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/fdad6862-34da-54fa-a9bd-aafb6cf3aca9.jsonl
---

# Session 12 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.9e — sign the manifest, and give the format a v2

**Completed:**
- `contracts/firmware-manifest-v2.md` — new spec; `sig` required, the canonical signed
  message pinned, key handling and the transition written down. v1 marked superseded and
  otherwise untouched, because a new *required* field is exactly what v1's own rule 3
  says needs a version bump rather than an amendment.
- `firmware/src/core/ed25519.{h,cpp}` — vendored TweetNaCl verify (public domain), one
  deliberate deviation (`gf0 = {0}`, C++ const-init). Verifies only; no signing on the
  node. **7,232 bytes of image.**
- `firmware/src/core/fw_manifest.{h,cpp}` — `sig` parsed as 128 lowercase hex,
  `canonicalMessage()` rebuilds the four fields in fixed order from parsed values,
  `verifyManifest()`.
- `firmware/src/esp32/ota_client.cpp` — `SOUNDINGS_OTA_PUBKEY` decoded from hex, checked
  before the radio comes on; signature verified before the image is requested at all.
- `firmware/platformio.ini` — the fleet public key, tracked, deliberately outside the
  `[ota]` section that `node_secret.ini` can blank. `kFwVersion` 262 → 263 in
  `firmware/src/esp32/main.cpp`.
- `gateway/soundings_gateway/ota.py` — parse + verify; `UpdatePolicy` requires a 32-byte
  key with no None escape and refuses to flag an unverified manifest.
- `gateway/tools/publish_firmware.py` — `--gen-key` (0600, `O_EXCL`, refuses to
  overwrite), `--key` required, signs before the atomic rename.
  `gateway/tools/bench_reply.py` gained `--ota-pubkey`, required with `--ota-dir`.
- `contracts/vectors/manifest-sig-v1.json` + `contracts/tools/gen_sig_vectors.py` — 12
  shared vectors grading all three implementations; the test key is published on purpose
  and regenerable from a stated phrase.
- `docs/decisions/DEC-013-*.md`, `docs/OTA.md` (signing, rotation, two new refusal modes),
  `.gitignore` (`*ota-signing-key*`), `gateway/pyproject.toml` (`cryptography>=41`).
- `docs/decisions/decision-record.schema.json` brought over from jig PR #6 —
  `check-decisions.mjs` already referenced the path and this repo never had the file, so
  **no decision record of any kind could be added here** until it landed. DEC-013 is the
  first record to declare `schema: 1`.

**Code review:** `@code-review` clean, 0 findings. `/security-review` clean, 0 findings —
1,600 differential checks of the vendored verifier against `cryptography` (400 keypairs ×
4 mutations, 0–200 byte messages), RFC 8032 §7.1 vectors, and 800k mutation-fuzz
iterations under ASan/UBSan. `/code-review ultra` not run (user-triggered only).

**Proof:** 185 native tests, 265 gateway tests, `npm run verify` green, three ESP32 envs
link. Hardware, on bee-grace 2026-08-28: a signed v263 accepted over the air by a node
still running v1 firmware with no verifier (`fw 262 → flags=0x0001`, then `seq 0, fw 263,
flags=0x0000`); then a manifest with a valid `sha256` and one flipped signature digit
refused with the flag forced — `ota: manifest SIGNATURE INVALID — nothing fetched,
nothing flashed`. The node never requested the image.

**PR:** [PR #87](https://github.com/mobiustripper42/soundings/pull/87)
**Points:** 8 — the issue said 5. One spec with three implementations that must agree
byte-for-byte, plus a new crypto primitive and a decision record. Coherent, so not split.
**Branch:** task/3.9e-sign-the-manifest
**Opened at:** 2026-08-28T12:52:00Z

**Next Steps:**
- ⚠ **Back up `~/.soundings/ota-signing-key` off this machine.** Highest-consequence loose
  end in the repo. It exists in exactly one place. Losing it means every deployed node —
  each carrying its public half — can never be updated over the air again, and the only
  fix is a USB trip per node. That is the precise cost issue #85 existed to remove.
- **Issue #82 — node-health on the wire** (reset-cause nibble + reboot counter) is the
  obvious next task: OTA aftercare, no hardware gate, and Session 10 deliberately kept it
  out of #85 because it changes the *uplink* packet contract rather than the downlink path.
- **The node is not parked.** It runs v263 on bee-grace at the 15-minute field cadence —
  the OTA flashed the `node` build, not `node_bench`. Gentler than the 20 s bench cadence
  Session 11 left it in, so parking is optional rather than owed.
- **Nothing left over from #85.** All eight acceptance criteria met, both hardware cases
  run. `/retro` should treat Phase 3.9 as closed unless #82 is folded into it.

**Context:**
- **The one-piece output style changed mid-session** (PR #88, merged). It loads at launch,
  so this session never ran under it — the next one is the first.
- ⚠ **Duplicated work with the operator, silently.** `docs/decisions/decision-record.schema.json`
  was copied from jig into PR #87 while the operator was landing the identical file in
  PR #86. Both merged; content is byte-identical so nothing broke. The lesson is the
  handoff, not the file: when the operator says "I'm fixing X in jig", X is theirs to
  land, and pulling it across here is a second path to the same commit.
- **Soundings could not accept ANY new decision record until PR #86.** `check-decisions.mjs`
  referenced `docs/decisions/decision-record.schema.json`, this repo never had it, and
  `drift.mjs` did not track it — so `schema: 1` failed on the missing file and legacy
  failed as not-in-baseline. DEC-013 is the first record here to declare `schema: 1`.
- **An empty `SOUNDINGS_OTA_PUBKEY` builds a 341 KB image; a real one builds 930 KB.**
  With a compile-time empty key the compiler proves `publicKey()` always false and drops
  the whole WiFi/HTTP/Update path as unreachable. That 589 KB gap is standing evidence
  that no reachable skip-verification branch exists — worth re-checking if it ever closes.
- **The OTA server tree is new.** `/srv/soundings-firmware/` was created this session
  (`sudo mkdir`, owned by the operator); `python3 -m http.server 8080` is run by hand from
  its parent and was stopped at the end. It is not a service.
- **`pio device monitor` cannot run non-interactively** — miniterm needs a TTY on stdin and
  dies with `termios.error: Inappropriate ioctl`. Reading a board's serial output from a
  session means a raw termios reader, the same shape as `bench_reply.py`'s `open_port`.
- **Permission policy is Current at both levels**; Session 11's stale user settings and the
  retired `SessionEnd` tape hook were fixed. Drift against jig is clean at jig-version 6.
