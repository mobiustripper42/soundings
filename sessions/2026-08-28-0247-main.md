---
session: 10
slug: main
branch: main
started: 2026-08-28T02:47:24Z
ended: 2026-08-28T02:59:37Z
points: 0
pr_numbers: []
status: closed
transcript: /home/eric/.claude/projects/-home-eric-soundings/1f07dea7-003b-585c-ab89-d63afd5c76bd.jsonl
---

# Session 10 — main

<!-- Task blocks appended by /kill-this, one per task. -->

A twelve-minute planning session on the wrong machine. No code, no PRs, one issue filed.

**Next Steps:**
- **Start [issue #85](https://github.com/mobiustripper42/soundings/issues/85) on bee-grace** —
  Phase 3.9e, sign the manifest, `points:5`. Filed this session from DEC-012's 2026-08-25
  amendment, which names both the fix and the trigger to do it.
- ⚠ **The hardware proof is the reason for the machine, and it is one specific case:**
  a manifest with a **valid `sha256` and a bad signature must be refused**. That is the case
  that passes today. Host tests cover the verification logic; only a real node fetching a
  real manifest proves the refusal.
- **Do not bundle #85 with issue #82** (node-health on the wire). Shared motivation — both
  are OTA aftercare — and no shared code: #85 is the OTA/downlink path, #82 changes the
  *uplink* packet contract. That is the "8 that is secretly two unrelated things" split,
  and each wants its own hardware proof.
- **Operator, still owed:** rotate the WiFi password, which now also means editing
  `firmware/node_secret.ini` (DEC-012 § "Credentials are build-time"). **The GitHub Support
  purge of orphaned commit `eb98539` was decided against this session** — Session 9's Next
  Steps carried it as outstanding; it is closed, not forgotten.
- **The node is very likely still transmitting.** See Context.

**Context:**
- ⚠ **This session ran on `mill-dev`, not bee-grace, and that is why nothing was built.**
  The boards live permanently cabled to bee-grace (`firmware/src/park/main.cpp:15`, DEC-009).
  `mill-dev` has no `/dev/ttyUSB*` and no `/dev/serial/by-id/` at all — the check is one
  command and it is worth running before planning any bench work.
- ⚠ **The node was left on bench firmware, awake every 20 s, and parking it was attempted
  from the wrong host.** Session 9's Next Steps recorded the parking command; this session
  re-issued it from `mill-dev`, where the port does not exist. Unless it has since been run
  **on bee-grace**, the node is still transmitting. The command is
  `pio run -e park -t upload --upload-port /dev/ttyUSB1` from `firmware/`
  (`firmware/platformio.ini:99`, `[env:park]`).
- **What `park` guarantees is silence, not microamps** — it sleeps the radio, then
  deep-sleeps the MCU with **no wake source at all**, so reflashing is the only way back.
  The deep-sleep current figure belongs to the disable list in issue #49, which park does
  not implement (`firmware/src/park/main.cpp:21-24`).
- **The no-hardware backlog, for a future session that lands on the wrong machine again:**
  issues #29, #30, #36, #60 and #82 are all gateway-side or contract work needing no board.
  Every open `phase:3` issue (#71, #49, #50, #51, #52 — 19 points) is parts-gated or field
  work. Phase 2 (#19–#25) is parked deliberately, `docs/PROJECT_PLAN.md:92`.
- **Housekeeping, both clean:** `node ../jig/scripts/drift.mjs .` reports nothing differs at
  jig-version 6; `settings-policy.mjs --all .` reports Current at both levels.
