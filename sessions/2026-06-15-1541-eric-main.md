---
session: 1
dev: eric
slug: main
branch: main
started: 2026-06-15T15:41:26Z
ended:
points:
pr_numbers: [11, 12, 13, 14, 15]
status: open
transcript: /home/eric/.claude/projects/-home-eric-soundings/12d90262-6594-4946-a94d-0882dfcf0979.jsonl
---

# Session 1 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.1 — firmware toolchain skeleton + native test harness (#4)

**Completed:**
- Resolved **D5 → PlatformIO** (mirrors tinkle). `firmware/platformio.ini` with `node` (Heltec WiFi LoRa 32 V3, ESP32-S3) + `native` (Unity) envs; native pinned `gnu++11` in confirmed lockstep with the node toolchain.
- `firmware/src/core/` — `IClock` const millis() seam (`iclock.h`), wrap-safe non-blocking `Elapsed` timer (`elapsed.h`), representative `ISoilMoisture` adapter seam (`isensor.h`, raw values per D1, declared-not-detected per DEC-002).
- `firmware/src/esp32/main.cpp` — minimal heartbeat entry proving core links under arduino-esp32.
- `firmware/test/test_native/` — 6 Unity tests + `fakes/{fake_clock.h,fake_sensor.h}` harness. Both envs build; 6/6 pass.
- Docs: D5 marked resolved in SPEC §12; CLAUDE.md Stack + Commands updated to real `pio` commands.

**Code review:** 3 findings, all addressed — const `millis()` seam, named `kHeartbeatMs`, confirmed gnu++11 lockstep (reviewer's C++17 assumption didn't hold for this platform).
**PR:** [#11](https://github.com/mobiustripper42/soundings/pull/11)
**Points:** 3
**Branch:** task/1.1-firmware-toolchain-skeleton
**Opened at:** 2026-06-16T21:00:57Z

## Task 2: Phase 1.2 — packet v1 wire contract + golden vectors (#5)

**Completed:**
- Resolved **D2 → DEC-003**: 12-byte LE header (`proto_ver`, `node_id`, `fw_version`, `seq`, `battery_mv`, `channel_mask`, `fault_mask`) + declared channel values in ascending channel-bit order + CRC-16/CCITT-FALSE.
- `contracts/packet-v1.md` — wire contract spec; `contracts/packet-v1-explained.md` — plain-English explainer (operator requested, byte-by-byte walkthrough); `contracts/README.md`.
- `contracts/vectors/packet-v1.json` — 7 golden vectors; `contracts/tools/gen_vectors.py` — reproducible generator (kept out of parser role).
- Manifest-on-the-wire via `channel_mask`; fault-vs-absent via parallel `fault_mask` (DEC-002); raw values on the wire (D1).
- Docs: DEC-003 added; D2 struck in SPEC §12.

**Code review:** @architect approved design (3 doc-only clarifications applied). @code-review caught header byte-count drift in prose (said 14, actual 12 — table/generator/JSON/vectors were correct); fixed in 4 places + added fault_mask⊆channel_mask assertion. Operator chose to review the code directly rather than vibe-code the contract.
**PR:** [#12](https://github.com/mobiustripper42/soundings/pull/12)
**Points:** 5
**Branch:** task/1.2-packet-v1-schema
**Opened at:** 2026-06-16T22:11:23Z

## Task 3: Phase 1.3 — C++ node-side packet serializer + native round-trip (#6)

**Completed:**
- `firmware/src/core/packet.{h,cpp}` — `serialize()`/`deserialize()`/`crc16_ccitt_false()`, `Packet` struct, channel-width registry. LE byte-by-byte (endian-agnostic), ascending-bit order, CRC-16/CCITT-FALSE. Defined rejection paths (TooShort/UnknownProto/UnknownChannel/BadFaultMask/LengthMismatch/BadCrc), output untouched on error.
- Platform-independent core: builds native **and** esp32-clean.
- `firmware/test/test_packet/test_packet.cpp` — loads the real `contracts/vectors/packet-v1.json` via ArduinoJson (test-only); byte-exact round-trip on all 7 vectors + 8 rejection tests. 15 native tests green.
- `firmware/platformio.ini` — ArduinoJson on native env.

**Code review:** 3 cleanup findings, all applied — bounds-guard channel accessors, cache `knownMask()`, add BadFaultMask + LengthMismatch tests. No correctness issues.
**PR:** [#14](https://github.com/mobiustripper42/soundings/pull/14)
**Points:** 3
**Branch:** task/1.3-cpp-serializer
**Opened at:** 2026-06-17T12:39:04Z

## Task 4: Phase 1.4 — Python gateway-side packet parser + pytest round-trip (#7)

**Completed:**
- `gateway/soundings_gateway/packet.py` — independent Python impl of packet v1: `parse()` (strict, raises `PacketError` subclasses), `decode()` (daemon boundary, logs+drops, never raises), `encode()` (round-trip + Phase 1.6 emitter), `crc16_ccitt_false()`, `Channel`/`Reading` dataclasses, channel registry. stdlib-only.
- `gateway/tests/test_packet.py` — parse/encode/round-trip parametrized over all 7 shared vectors + malformed-input robustness suite. 32 pytest cases green.
- `gateway/pyproject.toml` + `.gitignore`; pytest in `gateway/.venv` (Ubuntu 24.04 PEP-668, gitignored). CLAUDE.md gateway command updated.
- Both halves of the contract now meet: C++ (#6) and Python (#7) graded on the same `contracts/vectors/packet-v1.json`.

**Code review:** 4 consistency/cleanup findings, none blocking. Applied bytes-like input guard + 2 robustness tests (empty channel_mask, non-PacketError absorbed by decode).
**PR:** [#15](https://github.com/mobiustripper42/soundings/pull/15)
**Points:** 3
**Branch:** task/1.4-python-parser
**Opened at:** 2026-06-17T15:24:08Z
**Note:** proto_ver is the layout drop-gate; fw_version informational (logged, not dropped). Interpretation of the issue's "unknown fw-version" surfaced to operator.

**Next Steps:**
- #8 (Phase 1.5) — minimal sim server-stack compose (Mosquitto + provisional DB + Grafana, non-binding on D6).
- #9 (Phase 1.6) — end-to-end spine: synthetic-node emitter (reuses gateway `encode()`) → gateway → bus → DB → one moving chart. Closes Phase 1.

**Context:**
- PR #13 (`task/1.2b-record-packet-alternatives`) was a doc-only recovery: commit `cf6f263` (per-node-contract alternative + channel-ceiling v2 path) missed the PR #12 merge (landed one commit early at `9d2f006`); re-applied via cherry-pick. Now merged.
- Gateway tests need the venv: `cd gateway && .venv/bin/python -m pytest`.
