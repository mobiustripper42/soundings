---
session: 17
slug: 3.8c-ds18b20-headspace
branch: task/3.8c-ds18b20-headspace
started: 2026-09-09T23:22:50Z
ended:
points:
pr_numbers: [99, 100]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/c6c048bf-73f1-5849-9285-12e0e51bf26b.jsonl
---

# Session 17 — 3.8c-ds18b20-headspace

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.8c on silicon — the 1-Wire driver had never talked to a DS18B20

**Completed:**
- **The bench sitting for issue #94.** Checks 1, 2 and 3b pass, plus DEC-010's
  never-measured downlink direction. Checks 3 (below zero) and 4 (sleep current) still need a
  freezer and a meter.
- **Two firmware defects, neither findable from a host test.** The DS18B20 path had never
  once worked on any board, with 22 green tests over it the whole time.
  - **Bit timing.** `pinMode()`/`digitalWrite()` cost ~14 µs each on this chip. A write-1
    budgeted at 6 µs held the line low **35.7 µs against a 15 µs limit**, so every 1 bit went
    out as a 0 and no command was ever understood. The 500 µs reset swallowed it, so the bus
    reported a healthy presence pulse and returned nine bytes of `0xFF`.
    `esp32/onewire_bus.cpp` now drives the slots by register write: **6.9 µs**.
  - **The part lies about conversion-complete.** Answers read slots with 1 while still
    converting. Proven by waiting 800 ms blind and getting a real temperature where polling
    got the +85 °C power-on value. `core/ds18b20.cpp` floors the wait at the part's real
    conversion time, read from the config byte *before* converting (`dsConversionMs()`).
- **Removed the heal's verify — DEC-016 supersedes DEC-015.** It needed `Ve` to fall below the
  part's reset threshold in 50 ms; measured, it does not collapse until **between 50 and
  100 ms**. So it never reset the part, read back the scratchpad it had just written, and
  reported success unconditionally. `verifyDischargeMs` and `healSucceededLastRead()` gone; a
  heal wake now costs one rail cycle instead of two.
- **`src/ds18probe/`** — new committed bench instrument beside `hw13` and `park`. Three
  controls plus a stage-by-stage walk of `read()`. It is the only thing that can measure any
  of the above.
- **The fake was more honest than the hardware**, which is why the bug shipped. Gained
  `lieAboutConversionStatus()`, and now holds the +85 °C power-on value until a conversion
  actually completes. `ignorePowerCycles()` removed with the verify.
- `main.cpp` heal-flag message corrected — it read *"could not write the probe's EEPROM"* on a
  node that had healed perfectly, left over from a design where the flag was set on failure
  rather than on attempt. `kFwVersion` `0x0109` → `0x010D`.
- `HARDWARE_BUILD_PLAN.md` §8 bench sitting updated with results, and the board-identity
  warning strengthened: **a firmware banner identifies the image, not the board.**

**Code review:** 3 findings, all confirmed and fixed in `f697cb7`. `begin()` had silently
become a *required* precondition rather than a safe early call — the old `driveLow()` set
direction and level together so the open-drain guarantee re-proved itself every call, and the
register version does not. Plus a redundant `bus_.reset()` at the top of `read()`, and a
duplicated build-plan paragraph that was also carrying two stale claims.

**Verified on hardware:** `fault_mask 0`, both channels in one packet, 27.0 °C on channel 4
and ~2251 mm on channel 8, downlink heard 7/7 at ~1996 ms into a 250 ms window. Probe healed
`0x7F` → `0x1F`. 207/207 native tests, both field builds compile, all doc gates clean.

**PR:** [PR #99](https://github.com/mobiustripper42/soundings/pull/99) — pre-existing and
open from session 16; this task pushed two commits onto the same branch and logged them as a
[comment](https://github.com/mobiustripper42/soundings/pull/99#issuecomment-5626846295)
rather than opening a duplicate.
**Points:** 5
**Blocked:** ⚠ Issue #94 stays open for bench checks 3 and 4 — a below-zero reading and sleep
current — which need a freezer and a multimeter in series.
**Branch:** task/3.8c-ds18b20-headspace
**Opened at:** 2026-09-10T22:35:00Z

## Task 2: Phase 2.3 — VPD math, gateway-side

**Completed:**
- **`gateway/soundings_gateway/vpd.py`** — SHT45 raw ticks → °C/%RH → SVP → AVP → VPD per
  SPEC §5.3. Pure functions beside `tension.py` and `tank.py`, per DEC-004.
- **`_derive_air` wired into `_derive_bed`** (`gateway/soundings_gateway/derive.py`) — a
  canopy sensor now publishes `air_temp_c`, `air_rh_pct` and `vpd_kpa`.
- **`contracts/vectors/vpd-v1.json`** — golden vectors, computed with stdlib `math` rather
  than by calling the module under test.
- ⚠ **Scoped down on inspection, and this is the useful finding.** Issue #21 reads as a
  Phase 2 *firmware* task and almost none of it is. The wire was already finished —
  `packet-v1.md:82-83` defines bits 6/7 as SHT45 ticks with the exact conversions,
  `packet.h:31` sizes them, both parsers name them. Only the curve was missing, and
  DEC-004 puts that in Python. Re-estimated **3 → 2**.
- **`IHumidity` deliberately not added.** With no driver until Phase 6.1 and no tunnel-air
  preset, it would be an interface with a fake behind it and nothing on either end. Issue
  #19 stays open for it to land with the driver that needs it.
- **Also established: Phase 2 is not finished and cannot be retro'd yet.** Only #23 (run
  cycle) is unambiguously shippable-closed. #24 claims bed/tunnel-air/tank/rig presets and
  only `tankPreset` exists; #25 (Wokwi) has no `diagram.json`; #19 is short `IHumidity`.
- **Closed issue #98** as speculative — the condition it exists to surface has never been
  observed, and the one health problem this session actually demonstrated (a gateway dead
  for five days, unnoticed) is server-side and wouldn't be caught by it.

**Code review:** 2 findings, both confirmed and fixed in `0bbad7e`. The gating was
all-or-nothing where every neighbour in the file publishes each measurement on its own
merits — "VPD needs both" justifies gating the derived value, not hiding one that arrived;
the band check split per channel. And nothing tied `emitter._sht_temp_ticks` to
`vpd.air_temp_c`, so the sim's encoder and the gateway's decoder could drift apart
silently with the suite green. Now round-tripped.

**Two expectations were wrong when written and the tests caught them:** the hand-computed
SVP at 20 °C (2.338363 against 2.3382047), and a band check that condemned saturated air
as a broken sensor — the tick grid cannot land on exactly 100 %RH, so a plain `rh <= 100`
calls every cool morning a fault.

**Surface check:** the real sim emitter through encode → decode → derive shows VPD swinging
0.67–1.77 kPa across a simulated day and passing through the 0.8–1.2 tomato band.

**PR:** [PR #100](https://github.com/mobiustripper42/soundings/pull/100)
**Points:** 2
**Branch:** task/2.3-vpd-math
**Opened at:** 2026-09-11T04:05:00Z

**Next Steps:**

**Context:**
