---
session: 15
slug: main
branch: main
started: 2026-09-02T02:46:22Z
ended:
points:
pr_numbers: [97]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/389e30bd-7da9-564b-a85a-04367bc4aca3.jsonl
---

# Session 15 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.8b — A02YYUW on silicon, and issue #71 split in two

**Completed:**
- **Re-specced issue #71 from scratch** (operator asked for a fresh read, not session 14's
  notes) and **split it**: #71 is now 3.8b at `points:3`, and **issue #94** is 3.8c, the
  DS18B20 half, at `points:5`. The old 5 was an 8 — the DS18B20 is a new seam, a driver
  with CRC-8 and a conversion state machine, a bit-banged 1-Wire binding, a manifest type
  id and a second preset channel. Splitting costs almost no duplicated churn and gets HW-13
  answered without waiting on work that is gated on nothing.
- **Chose the sensor pins, which had never been chosen.** Grepped `docs/`, `contracts/`,
  `firmware/src/`: no GPIO for the A02YYUW's UART RX or the DS18B20's DQ existed anywhere.
  Pulled the WSL V3 Rev 1.1 datasheet and took the header map from Table 2.2-2. **UART RX =
  GPIO6 (J3 pin 18); DS18B20 DQ = GPIO7 (J3 pin 17)**, operator-confirmed.
- `firmware/src/esp32/uart_bytesource.{h,cpp}` — `Serial1` behind `IByteSource`, 9600 8N1.
- `firmware/src/esp32/vext_rail.{h,cpp}` — `IPowerRail` on GPIO36, active LOW, deep-sleep
  pad hold.
- `firmware/src/hw13/main.cpp` + `[env:hw13]` — the bench program that answers HW-13.
- `firmware/src/esp32/main.cpp` — `UnfittedDistance` replaced by a live `A02yyuwDistance`;
  `kFwVersion` bumped to `0x0108`.
- `docs/HARDWARE_BUILD_PLAN.md` §8 — the wiring section for the sensor run.

**Code review:** 1 finding, fixed in `fc64263` — `kFwVersion` was left at `0x0107` while the
image changed behaviour on target. Five deep-verification items (GPIO hold sequencing across
deep sleep, static-init order, `uint16_t` wrap in the bench tally, the `readByte()` contract
and its `-1`→`0xFF` hazard, blocking in the run path) all came back clean.
**PR:** [PR #97](https://github.com/mobiustripper42/soundings/pull/97)
**Points:** 3
**Blocked:** ⚠ **HW-13 is unanswered — the sensor is not on the board yet.** The PR
deliberately omits `closes #71`; two of that issue's acceptance criteria are the bench
sitting itself and recording step-5 check 4. The issue stays open until the sitting happens.
**Branch:** task/3.8b-a02yyuw-on-silicon
**Opened at:** 2026-09-03T00:26:00Z

**Next Steps:**

**Context:**
