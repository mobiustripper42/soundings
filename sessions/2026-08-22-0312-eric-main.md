---
session: 8
dev: eric
slug: main
branch: main
started: 2026-08-22T03:12:01Z
ended:
points:
pr_numbers: [72, 74]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-soundings/85e2f5db-e395-5d29-9c6b-e6b31539a9be.jsonl
---

# Session 8 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Housekeeping (not a task — no PR)

- **Phase 3.8 was split into 3.8a and 3.8b** at the operator's call. Issue #47 was
  retitled to 3.8a and its acceptance criteria rewritten; **issue #71 is new** and
  carries the bring-up half. This also fixed the stale criterion Session 7 flagged —
  #47 still said *"Sensor rail switched by the P-FET, not Vext"*, backwards since the
  2026-08-20 bench session.
- ⚠ **The split is a re-estimate, not bookkeeping.** 3.8 was 3 points; 3.8a (3) +
  3.8b (5) is 8. The plan row already carried "the 3 may now be light" — it was light
  by more than a little. Operator confirmed 5 for 3.8b.
- **`PROJECT_PLAN.md:152` still describes 3.8 as one task pointing at issue #47.** Left
  alone deliberately — the plan is untouched mid-phase and `/retro` reconciles it. Phase 3
  now stands at 24 points across six open issues.
- **Drift re-checked and clean.** Session 7's briefing reported five session skills
  differing from seeds; re-running after `git -C ../seeds pull` showed nothing differs.
  The stale side was **seeds**, not this repo — its checkout was two days behind and
  fast-forwarded to 902c711 (seeds PR #205) at 08:25 EDT, picking up the same
  one-session-one-worktree change soundings adopted on 2026-08-21 in PR #67.

## Task 1: Phase 3.8a — A02YYUW frame parser + read strategy behind IDistance

**Completed:**
- **Two new seams** — `firmware/src/core/ibytesource.h` (non-blocking `readByte`) and
  `firmware/src/core/ipowerrail.h` (`on`/`off`), with fakes. The Heltec's active-LOW
  Vext polarity stays in the ESP32 binding, not the seam: F-13's fallback puts an
  AO3401 back on the BOM sourced from VBAT, which is the same rail with the opposite
  sense, and a seam that leaked polarity would have to be rewritten for it.
- **`firmware/src/core/a02yyuw.{h,cpp}`** — a byte-at-a-time resync parser and the read
  cycle. One `IDistance::read()` is one complete cycle: rail on, settle, discard 3,
  collect 5, median, rail off. Bounded by a deadline; a silent sensor returns a failed
  reading rather than holding the node awake.
- **The protocol was sourced, not inferred.** `SUM = (Header + Data_H + Data_L) & 0x00FF`
  and the `FF 07 A1 A7` → 1953 mm worked example come from DFRobot's own protocol page —
  the URL already cited in `CHAT_HANDOFF.md` HW-08. Fetched and read this session rather
  than reconstructed from the part's family.
- **21 new tests** — 16 in `firmware/test/test_a02yyuw/`, 5 grading the new fakes in
  `test_adapters`. Full native suite 92/92; `pio run -e node` clean at 7.9% flash;
  `npm run verify` green.
- **`FakeClock` gained an opt-in `autoAdvance`** (default off, so five other suites are
  untouched). A loop whose only escape is a deadline never terminates against a clock
  that never ticks — the timeout test would have hung the suite instead of failing it.
- **`docs/tank-level-sensor.md`** — five-line pointer from the read-strategy section to
  the implementation.

⚠ **A false green was caught during development, before review.**
`test_read_times_out_on_a_silent_sensor` passed against the do-nothing stub, because
`{0, false}` is also what a driver that never tries returns. It was green while asserting
nothing. Strengthened with rail-cycle and elapsed-deadline assertions until it failed,
then implemented against it. This is the exact failure the workflow's "watch it fail for
the reason you expect" step exists to catch, and it only surfaced because the stub run
happened — writing the test after the code would have shipped it looking checked.

**Test-design note worth keeping:** the frame-accounting tests run with the settle window
switched off, deliberately. `FakeByteSource` hands over a byte per poll, so against a
ticking clock the stream arrives ~25× faster than a real 9600-baud sensor and the settle
window swallows the whole script. Slowing the fake to real cadence only moves the problem
— the window would end mid-frame, one frame would be lost to resync, and each test's
expected median would silently depend on how many times the driver's loop read the clock.
The window gets its own test proving it by contrast instead.

**Decision record:** `grep -rli` on `a02yyuw`, `distance`, `median`, `read strategy`,
`ultrasonic` returned DEC-004/005/007/008 by keyword only. Nothing here changes what any
decided — DEC-007 puts the temperature correction gateway-side and this ships raw
millimetres. **No new decision, no amendment.** Flagged for review: whether "the read
strategy lives inside the driver" warranted a DEC. I said no (internal structure behind
an existing seam, documented at the seam); Session 7 had a near-identical call escalated
by `@code-review` and it went the other way. The reviewer did not raise it this time.

**Code review:** 3 findings, all cleanup-tier, all real, all fixed in `c8ea6c9`. The one
with teeth: `FakeByteSource::consumed()` was built so a test could prove the driver stops
reading once it has its five frames, and **no test ever called it** — an unasserted claim
in a fake reads exactly like a checked one. Also `static const` → `constexpr` on
`kMaxSampleFrames` (node build re-run, since in-class `constexpr` statics are where
ODR-use bites), and `badChecksums()`'s cumulative-across-lifetime semantics stated rather
than left to be inferred. `/security-review` not run — no blast-radius trigger, and
`.claude/CLAUDE-context.md` carries no `## Blast-Radius Triggers` section, so the four
generic ones applied.

**PR:** [PR #72](https://github.com/mobiustripper42/soundings/pull/72) — `closes #47`
verified via GraphQL to resolve to issue #47 only; issue #71 stays open.
**Points:** 3
**Branch:** task/3.8a-a02yyuw-parser
**Opened at:** 2026-08-22T13:56:41Z

## Housekeeping 2 — Phase 3.9 split, and an architect review

- **Issue #48 retitled to 3.9a** (`points:5` → `points:3`); **issue #73 is new**, 3.9b at
  `points:8`. Same shape as the 3.8a/3.8b split. Phase 3 now stands at 32 points across
  seven open issues.
- **`@architect` reviewed the 3.9 plan** (four targeted questions, not a general review)
  and corrected three of my calls — recorded here because two of them are reusable:
  1. **The OTA/spec reading was wrong.** `SPEC.md:95` says *"Don't **build** OTA before
     there's firmware worth updating"* — an injunction against building, not a
     description of field behaviour. I had ruled it needed no spec amendment. It does:
     §3 and §8, not §4.
  2. **The framing vectors were cargo-culted from packet-v1.** Dropped.
  3. **The split argument I'd twice rejected was better than mine.** The seam is where
     the *proof* changes — 3.9a is `pytest`-only, 3.9b needs a physical bench sitting. So
     the Python end merges before the bench session, and when nothing decodes (the
     expected first outcome of a radio bring-up) the search space is the radio rather
     than seven items across two languages. **Bench sessions are the scarce resource in
     Phase 3, not points.**
- ⚠ **Both ESP32s are on `bee-grace` and both CP2102s report serial `0001`**, so
  `/dev/serial/by-id/` holds one symlink for two boards and `ttyUSB0`/`ttyUSB1` are
  assigned in plug order. Identified via the Meshtastic CLI: **`/dev/ttyUSB0` = "home"**
  (`ac:a7:04:39:0d:c0`), **`/dev/ttyUSB1` = "tank"** (`ac:a7:04:04:e5:48`), both on
  Meshtastic 2.7.26. Not worth a udev rule — 3.9b writes both firmwares, so the gateway
  can announce itself and the daemon can probe.
- **Operator decisions recorded:** node TX driver belongs in 3.9b; one DEC for the link
  (modem parameters + framing together); TX power is a config value with a low bench
  setting; OTA is triggered over the radio, not on a timer — the node holds a short
  receive window after each transmit, and WiFi comes up only when there is something to
  fetch; **bricking-means-USB is an accepted cost on the field node**, and nothing may
  complicate the OTA design to avoid it.
- **The gateway venv did not exist on this machine** and every Python proof depends on
  it. Created before 3.9a started.

## Task 2: Phase 3.9a — serial framing contract + SerialPacketSource

**Completed:**
- **`contracts/serial-framing-v1.md`** — a three-field envelope (`0xA5 0x5A`, u8 length,
  payload) for carrying packet-v1 over the gateway board's USB link, plus the five-step
  read algorithm and the three resync cases it must survive: board reset, reset
  mid-frame, and daemon restart landing mid-frame.
- **`firmware/src/core/serial_framing.{h,cpp}`** — the encoder, in `src/core` rather than
  the gateway sketch so `pio test -e native` compiles it. A framer in `src/esp32` would
  meet its decoder for the first time on a bench.
- **`gateway/soundings_gateway/framing.py`** — `SerialFramer`, the resync state machine.
  Emits *candidates*; the payload CRC settles what is real, on the path `decode()`
  already provides for radio-corrupted packets.
- **`SerialPacketSource`** behind the existing `IPacketSource`, taking anything with
  `.read(n)` via a structural `ByteStream` protocol — **no pyserial dependency added.**
  Two stream conventions documented on the class: `b""` means quiet (a read timeout, not
  EOF — ending iteration there would stop the daemon the first quiet minute), `EOFError`
  means done (so a test can script a finite stream with no test-only flag on the
  production class).
- **32 new tests** — 9 native, 23 pytest. Suites: 134 pytest, 101 native, node build
  clean at 7.9% flash, `npm run verify` green.

**Three design calls, with their reasons:** no frame checksum (the payload already
carries CRC-16/CCITT-FALSE and USB's link layer is CRC-protected; a second checksum is a
second thing to get wrong); no shared vector file, departing from packet-v1 deliberately
(one-way, fixed-shape, no round trip to pin, and a framing bug means *nothing* decodes on
the first bench try rather than a plausible wrong number for a season); encoder not
behind an adapter seam (a seam earns its keep where a real implementation and a fake both
exist — this is a pure function over bytes).

⚠ **Three false greens caught during development, before review.** All three C++
"refuses bad input" tests passed against a stub returning 0 unconditionally — *a stub
that refuses everything satisfies every refusal assertion.* Each now also asserts the
adjacent legal length succeeds, pinning the boundary rather than a general willingness to
say no. **Same class as the one caught in PR #72, which makes it a pattern rather than an
accident:** refusal tests are the ones that survive a null implementation, and they need
a positive case in the same test or they assert nothing.

**A test also found a property I had documented wrong**, before review saw it: a
truncated frame whose `LEN` byte was valid *absorbs* the bytes behind it rather than
simply failing to complete. Contract and test both corrected to describe what actually
happens.

**Code review:** 2 findings. **The real one is arithmetic I never did.** I wrote that the
absorption cost was "at most one following frame lost." The smallest complete frame is
3 + 14 = 17 bytes and the largest window is 46, so ⌊46 ÷ 17⌋ = **two** whole frames fit —
30 minutes of tank level at the 15-minute cadence, and precisely the number someone
staring at a gap in the chart would trust. The contract now states the bound in *bytes*
(what the code guarantees) and derives the frame count beside it, pinned by a test that
builds the worst case explicitly. The second finding asked whether a new wire contract
needs its own DEC; answered in the PR — framing and modem parameters are one subject per
the operator's call, and that DEC lands in 3.9b, which stacks directly on this branch.

**PR:** [PR #74](https://github.com/mobiustripper42/soundings/pull/74) — `closes #48`
verified via GraphQL to resolve to issue #48 only.
**Points:** 3
**Branch:** task/3.9a-serial-framing
**Opened at:** 2026-08-23T10:54:11Z

**Next Steps:**

**Context:**
