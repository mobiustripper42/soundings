---
session: 3
dev: eric
slug: main
branch: main
started: 2026-08-10T00:31:19Z
ended:
points:
pr_numbers: [54]
status: open
transcript: /home/eric/.claude/projects/-home-eric-soundings/d0719939-2aa3-4574-9ad4-aa589febe290.jsonl
---

# Session 3 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.3 — tank-node adapter seam + fakes (issue #42)

**Completed:**
- Materialized Phase 3 first: issues #40–#52 (47 pts), `docs/PROJECT_PLAN.md` Issue column filled. Issue #40 (3.1, hardware plan) created and closed on the spot — it shipped 2026-08-08, and skipping it would have undercounted the phase by 3 pts in the DEC-S026 throughput math.
- `firmware/src/core/{idistance,iradio,ibattery,isleeper}.h` — the tank subset of the adapter seam. Raw mm on the wire (D1); `battery_mv` stays a header field; `TxResult { Ok, Busy, Failed }`.
- `firmware/test/fakes/{fake_distance,fake_radio,fake_battery,fake_sleeper}.h` — plus `fake_clock.h`/`fake_sensor.h` moved up from `test/test_native/fakes/` so 3.4 and 3.5 can use them from their own suites. Relative includes sufficed; `platformio.ini` untouched.
- `firmware/test/test_adapters/test_adapters.cpp` — 17 tests. Suite total 15 → 32, all green; `pio run -e node` clean.
- `firmware/src/esp32/main.cpp` — +4 includes, so the node build actually compiles the new headers rather than the claim going unchecked.

**Three decisions, specced and confirmed before the branch was cut:**
1. Sleep is its own seam, not a method on `IClock` — ESP32 deep sleep does not return, it resets the MCU.
2. `IRadio` takes bytes, not a `Packet` — keeps the codec/transport weld out of 3.9.
3. Fakes live in a shared `test/fakes/`.

**Code review:** 2 findings, neither blocking, neither acted on. (1) `sleepFor()` returns `void`, so nothing stops 3.4 writing code after it that only breaks on hardware — every alternative was worse, so it is carried forward to issue #43 as a review convention. (2) `FakeRadio::count_` grows past capacity by design — `sentCount()` means attempts, and `overflowed()` makes the storage limit visible.
**PR:** [PR #54](https://github.com/mobiustripper42/soundings/pull/54)
**Points:** 3
**Branch:** task/3.3-adapter-seam-tank
**Opened at:** 2026-08-10T01:29:00Z

**Next Steps:**

**Context:**
